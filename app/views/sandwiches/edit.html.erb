<%= form_for @sandwich do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :Ingredients %>
  <%= f.collection_check_boxes(:ingredient_ids, @ingredients, :id, :name) %>
  <%= f.submit %>
<% end %>